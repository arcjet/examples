services:
  langchain-agent:
    build: .
    command: npm run dev
    env_file:
      - .env.local
    labels:
      - dev.orbstack.domains=langchain-agent.arcjet-examples.orb.local
    ports:
      - 3000
    volumes:
      - .:/app
      - langchain-agent_node_modules:/app/node_modules

volumes:
  langchain-agent_node_modules:
