services:
  claude-agent-sdk:
    build: .
    command: npm run dev
    env_file:
      - .env.local
    labels:
      - dev.orbstack.domains=claude-agent-sdk.arcjet-examples.orb.local
    ports:
      - 3000
    volumes:
      - .:/app
      - claude-agent-sdk_node_modules:/app/node_modules

volumes:
  claude-agent-sdk_node_modules:
