services:
  genkit-agent:
    build: .
    command: npm run dev
    env_file:
      - .env.local
    labels:
      - dev.orbstack.domains=genkit-agent.arcjet-examples.orb.local
    ports:
      - 3000
    volumes:
      - .:/app
      - genkit-agent_node_modules:/app/node_modules

volumes:
  genkit-agent_node_modules:
