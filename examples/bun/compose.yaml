services:
  bun:
    build: .
    command: bun run dev
    env_file:
      - .env.local
    labels:
      - dev.orbstack.domains=bun.arcjet-examples.orb.local
    ports:
      - 3000
    volumes:
      - .:/app
      - bun_node_modules:/app/node_modules

volumes:
  bun_node_modules:
