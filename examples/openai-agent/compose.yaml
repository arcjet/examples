services:
  openai-agent:
    build: .
    command: npm run dev
    env_file:
      - .env.local
    labels:
      - dev.orbstack.domains=openai-agent.arcjet-examples.orb.local
    ports:
      - 3000
    volumes:
      - .:/app
      - openai-agent_node_modules:/app/node_modules

volumes:
  openai-agent_node_modules:
